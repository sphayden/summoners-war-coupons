{
  "name": "summoners-war-coupons",
  "version": "1.0.0",
  "description": "Community platform for Summoners War coupon codes",
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "dotenv": "^17.2.1",
    "uuid": "^9.0.1"
  },
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
